{
  "name": "@sgawarat/blog-theme",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "apps": "pnpm --filter ./apps/**",
    "libs": "pnpm --filter ./libs/**",
    "libs:build": "pnpm --filter ./libs/** build",
    "prepare": "pnpm --filter ./libs/** build",
    "ci:version": "pnpm changeset version",
    "ci:publish": "pnpm changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@changesets/cli": "^2.29.7"
  }
}
